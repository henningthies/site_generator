<ul>
  <% Site.all.each do |site| %>
    <li><%= link_to site.title, site_path(site) %></li>
  <% end %>
</ul>

<%= form_for Site.new do |f| %>
  <%= f.text_field :title %>
  <%= f.submit %>
<% end %>
